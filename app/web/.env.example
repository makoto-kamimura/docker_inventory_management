# Web (Next.js) フロントエンドの環境変数。
# `cp app/web/.env.example app/web/.env.local` してから必要に応じて編集する。
# NEXT_PUBLIC_* はクライアントバンドルにも含まれる点に注意 (秘密を入れない)。

# Laravel API のベース URL。未設定時は http://localhost:8000 にフォールバック。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
